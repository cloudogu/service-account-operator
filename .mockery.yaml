with-expecter: true
all: true
exclude: ["vendor"]
recursive: true
inpackage: true
include-auto-generated: false
mockname: "{{.Mock}}{{.InterfaceName | camelcase }}"
filename: "mock_{{.InterfaceName}}_test.go"
dir: "{{.InterfaceDir}}"

packages:
  github.com/cloudogu/service-account-operator:
    config:
      recursive: True